# Рефакторинг проекта PathOfQuality

## Обзор изменений

Проект был полностью реструктурирован для улучшения читаемости, поддерживаемости и масштабируемости кода.

## Новая структура модулей

### 1. `src/utils/` - Утилитарные функции
Выделены общие утилиты из `app.py`:

- **`settings.py`** - Управление настройками (загрузка, сохранение, дефолты)
- **`screen.py`** - Работа с экраном (определение размера)
- **`roi.py`** - Вычисление Region of Interest

### 2. `src/ui/` - Пользовательский интерфейс

#### Разделение `hud.py` (947 строк → модульная структура):

- **`styles.py`** - Конфигурация стилей ttk виджетов
- **`hud.py`** (новый) - Упрощённый главный класс BuffHUD (~280 строк)

#### `tabs/` - Модули вкладок:
- **`monitoring_tab.py`** - Вкладка мониторинга (сканирование, статус)
- **`settings_tab.py`** - Вкладка настроек (ROI, язык, overlay)
- **`library_tab.py`** - Обобщённая вкладка для баффов/дебаффов

#### `components/` - Переиспользуемые компоненты:
- **`library_tree.py`** - TreeView для отображения библиотеки баффов/дебаффов

#### Разделение `icon_mirrors.py` (387 строк → 3 модуля):
- **`mirror_window.py`** - Класс отдельного окна-зеркала
- **`positioning.py`** - Логика позиционирования и привязки к сетке
- **`icon_mirrors.py`** (новый) - Главный класс управления зеркалами (~180 строк)

### 3. `src/core/` - Основная логика приложения

- **`application.py`** - Класс Application с главным циклом событий

### 4. `app.py` - Новая точка входа

Упрощённый главный файл (~20 строк), использующий модульную архитектуру.

## Преимущества новой структуры

### 1. Разделение ответственности (SRP)
- Каждый модуль отвечает за одну область функциональности
- Легче понять назначение каждого файла

### 2. Переиспользуемость
- `LibraryTreeView` используется для баффов и дебаффов
- Компоненты можно использовать независимо

### 3. Тестируемость
- Модули легче покрыть юнит-тестами
- Меньше зависимостей между компонентами

### 4. Масштабируемость
- Легко добавить новые вкладки
- Просто расширить функциональность отдельных модулей

### 5. Читаемость
- Файлы меньше 300 строк вместо 900+
- Понятная иерархия модулей

## Карта миграции

### Старая структура → Новая структура

```
app.py (251 строк)
├── get_screen_size()          → src/utils/screen.py
├── load_settings()            → src/utils/settings.py
├── save_settings()            → src/utils/settings.py
├── compute_roi()              → src/utils/roi.py
└── main()                     → src/core/application.py + app.py (новый)

src/ui/hud.py (947 строк)
├── Стили ttk                  → src/ui/styles.py
├── Вкладка мониторинга        → src/ui/tabs/monitoring_tab.py
├── Вкладка настроек           → src/ui/tabs/settings_tab.py
├── Вкладки баффов/дебаффов    → src/ui/tabs/library_tab.py
├── TreeView компонент         → src/ui/components/library_tree.py
└── Главный класс BuffHUD      → src/ui/hud.py (новый)

src/ui/icon_mirrors.py (387 строк)
├── Класс _Mirror              → src/ui/mirror_window.py
├── Логика снэпа к сетке       → src/ui/positioning.py
└── IconMirrorsOverlay         → src/ui/icon_mirrors.py (новый)
```

## Использование

### Запуск приложения:
```bash
python app.py
```

## Следующие шаги

1. ✅ Создать утилиты (settings, screen, roi)
2. ✅ Разделить HUD на модули
3. ✅ Разделить icon_mirrors
4. ✅ Создать core/application
5. ✅ Создать новый app.py
6. ⏳ Тестирование новой структуры
7. 🔄 Замена старых файлов на новые (после тестирования)

## Совместимость данных

- Настройки (`settings.json`) полностью совместимы
- Библиотека (`assets/buffs.json`) совместима
- Все данные сохранены

## Дополнительные улучшения

### Потенциальные улучшения:
1. Добавить логирование вместо print
2. Создать конфигурацию для типов (TypedDict)
3. Добавить валидацию настроек
4. Создать абстракции для матчеров
5. Добавить dependency injection

## Структура файлов

```
PathOfQuality/
├── app.py                    # Точка входа приложения
├── src/
│   ├── utils/               # Утилиты
│   │   ├── settings.py
│   │   ├── screen.py
│   │   └── roi.py
│   ├── core/                # Основная логика
│   │   └── application.py
│   ├── ui/                  # Интерфейс
│   │   ├── styles.py
│   │   ├── hud.py           # Главное окно HUD
│   │   ├── icon_mirrors.py  # Зеркала иконок
│   │   ├── mirror_window.py
│   │   ├── positioning.py
│   │   ├── overlay.py
│   │   ├── roi_selector.py
│   │   ├── tray.py
│   │   ├── tabs/
│   │   │   ├── monitoring_tab.py
│   │   │   ├── settings_tab.py
│   │   │   └── library_tab.py
│   │   ├── components/
│   │   │   └── library_tree.py
│   │   └── dialogs/
│   │       ├── buff_editor.py
│   │       └── localize_dialog.py
│   ├── buffs/
│   │   └── library.py
│   ├── capture/
│   ├── detector/
│   └── i18n/
└── assets/
```

