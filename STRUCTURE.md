# Структура проекта PathOfQuality

## 📁 Организация кода

### Модульная архитектура

Проект организован по принципу разделения ответственности (Separation of Concerns):

```
PathOfQuality/
│
├── 📄 app.py                  # Точка входа (20 строк)
├── 📄 REFACTORING.md          # Документация по рефакторингу
├── 📄 STRUCTURE.md            # Этот файл
│
├── 📁 src/
│   │
│   ├── 📁 utils/              # Утилиты общего назначения
│   │   ├── settings.py        # Управление настройками
│   │   ├── screen.py          # Работа с экраном
│   │   └── roi.py             # Вычисление ROI
│   │
│   ├── 📁 core/               # Ядро приложения
│   │   └── application.py     # Главный класс Application
│   │
│   ├── 📁 ui/                 # Пользовательский интерфейс
│   │   ├── styles.py          # Стили ttk виджетов
│   │   ├── hud.py             # Главное окно HUD
│   │   ├── icon_mirrors.py    # Зеркала иконок
│   │   ├── mirror_window.py   # Отдельное окно зеркала
│   │   ├── positioning.py     # Логика позиционирования
│   │   ├── overlay.py         # Оверлей ROI
│   │   ├── roi_selector.py    # Выбор ROI
│   │   ├── tray.py            # Системный трей
│   │   │
│   │   ├── 📁 tabs/           # Вкладки UI
│   │   │   ├── monitoring_tab.py   # Мониторинг
│   │   │   ├── settings_tab.py     # Настройки
│   │   │   └── library_tab.py      # Библиотека
│   │   │
│   │   ├── 📁 components/     # Переиспользуемые компоненты
│   │   │   └── library_tree.py     # TreeView для библиотеки
│   │   │
│   │   └── 📁 dialogs/        # Диалоговые окна
│   │       ├── buff_editor.py      # Редактор баффов
│   │       └── localize_dialog.py  # Локализация
│   │
│   ├── 📁 buffs/              # Управление библиотекой
│   │   └── library.py         # CRUD для баффов/дебаффов
│   │
│   ├── 📁 capture/            # Захват экрана
│   │   ├── base_capture.py    # Базовый класс
│   │   └── mss_capture.py     # Реализация через MSS
│   │
│   ├── 📁 detector/           # Детектирование
│   │   ├── template_matcher.py     # Поиск шаблонов
│   │   └── library_matcher.py      # Поиск по библиотеке
│   │
│   └── 📁 i18n/               # Интернационализация
│       └── locale.py          # Управление языками
│
└── 📁 assets/                 # Ресурсы
    ├── buffs.json             # База баффов/дебаффов
    ├── templates/             # Шаблоны для поиска
    └── i18n/                  # Переводы
        ├── en.json
        └── ru.json
```

## 🔄 Поток данных

```
┌─────────────┐
│   app.py    │  Инициализация
└──────┬──────┘
       │
       v
┌─────────────────────────────┐
│  core/application.py        │
│  - Главный цикл             │
│  - Координация компонентов  │
└──┬──────────────────────┬───┘
   │                      │
   v                      v
┌──────────┐        ┌─────────────┐
│ capture/ │───────>│ detector/   │
│ (MSS)    │        │ (OpenCV)    │
└──────────┘        └──────┬──────┘
                           │
                           v
                    ┌─────────────────┐
                    │  ui/hud.py      │
                    │  - tabs/        │
                    │  - components/  │
                    └────────┬────────┘
                             │
                             v
                    ┌─────────────────┐
                    │ icon_mirrors.py │
                    │ (Overlay Windows)│
                    └─────────────────┘
```

## 📊 Метрики кода

### До рефакторинга:
- `app.py`: 251 строк (смешанная ответственность)
- `hud.py`: 947 строк (монолит)
- `icon_mirrors.py`: 387 строк (связанность)

### После рефакторинга:
- `app.py`: 20 строк ✅
- `core/application.py`: ~250 строк (чистая логика)
- `ui/hud.py`: ~280 строк (координация)
- `ui/tabs/*`: ~150-200 строк каждый
- `ui/components/*`: ~250 строк
- `ui/icon_mirrors.py`: ~180 строк
- `ui/mirror_window.py`: ~150 строк
- `ui/positioning.py`: ~80 строк

**Средний размер модуля: ~150 строк** (было ~500)

## 🎯 Принципы архитектуры

### 1. Single Responsibility Principle (SRP)
Каждый модуль отвечает за одну область:
- `settings.py` → только настройки
- `monitoring_tab.py` → только вкладка мониторинга
- `library_tree.py` → только TreeView библиотеки

### 2. Don't Repeat Yourself (DRY)
- `LibraryTreeView` используется для баффов и дебаффов
- `PositioningHelper` переиспользуется для всех окон
- Стили определены в одном месте (`styles.py`)

### 3. Separation of Concerns
- **UI** отделён от **логики** отделён от **данных**
- `ui/` → только интерфейс
- `core/` → только бизнес-логика
- `buffs/` → только данные

### 4. Dependency Injection
- Компоненты получают зависимости через конструктор
- Легко заменить реализацию (например, capture provider)

## 🔧 Как добавить новую функцию

### Добавить новую вкладку:
1. Создать класс в `src/ui/tabs/my_tab.py`
2. Зарегистрировать в `src/ui/hud.py`

### Добавить новый детектор:
1. Создать класс в `src/detector/my_detector.py`
2. Использовать в `src/core/application.py`

### Добавить новую утилиту:
1. Создать функцию в `src/utils/my_util.py`
2. Импортировать где нужно

## 📝 Пример использования

```python
# Инициализация приложения
from src.core.application import Application

app = Application(settings_path='settings.json')
app.initialize(roi)
app.run()
```

## 🧪 Тестирование

Модульная структура упрощает тестирование:

```python
# Тест отдельного компонента
from src.utils.settings import load_settings

def test_load_settings():
    settings = load_settings('test_settings.json')
    assert settings['language'] == 'en'
```

## 📚 Дополнительно

### Старые файлы (резервные копии):
- `app_old.py` - старый app.py
- `src/ui/hud_old.py` - старый HUD
- `src/ui/icon_mirrors_old.py` - старые зеркала

### Можно удалить после проверки работы новой версии.

## 🚀 Запуск

```bash
# Запуск приложения
python app.py

# Или через батник
run_app.bat
```

## ⚙️ Конфигурация

Настройки в `settings.json`:
- `capture` - провайдер захвата
- `roi` - регион интереса
- `threshold` - порог совпадения
- `ui` - параметры интерфейса
- `language` - язык интерфейса

## 🌐 Локализация

Добавить новый язык:
1. Создать `assets/i18n/XX.json`
2. Добавить в `src/ui/tabs/settings_tab.py`

